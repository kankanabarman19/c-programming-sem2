#include<stdio.h>
//finding sum of square of the first 10 natural numbers
#include<math.h>
int main()
{
	float sum;
	sum=pow(1,2)+pow(2,2)+pow(3,2)+pow(4,2)+pow(5,2)+pow(6,2)+pow(7,2)+pow(8,2)+pow(9,2)+pow(10,2);
	printf("the sum of the squares of the first 10 natural numbers is:%f\n",sum);
	return 0;
}
